/**
 * promptForTipPercent - description
 *
 * @return {type}  description
 */
function promptForTipPercent() {

}


/**
 * computeTip - description
 *
 * @param  {type} billAmount description
 * @param  {type} tipPercent description
 * @return {type}            description
 */
function computeTip(billAmount, tipPercent) {
  var tipAmount;
  var totalAmount;

  tipAmount = billAmount * tipPercent;
  totalAmount = billAmount + tipAmount;
  return totalAmount;
}

/**
 * displayTotals - description
 *
 * @return {type}  description
 *
 */
function displayTotals() {

}
